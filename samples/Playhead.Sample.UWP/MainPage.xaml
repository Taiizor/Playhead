<Page
    x:Class="Playhead.Sample.UWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Grid Padding="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Playhead Media Controller" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <Border Grid.Row="1" Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}" CornerRadius="8" Padding="20" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock x:Name="TitleText" Text="No Media Selected" FontSize="20" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                <TextBlock x:Name="ArtistText" Text="Select a session below" FontSize="16" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" Margin="0,5,0,0" TextTrimming="CharacterEllipsis"/>
                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button Content="⏮ Prev" Click="Prev_Click" Width="80" Margin="0,0,10,0"/>
                    <Button Content="▶ Play" Click="Play_Click" Width="80" Margin="0,0,10,0"/>
                    <Button Content="⏸ Pause" Click="Pause_Click" Width="80" Margin="0,0,10,0"/>
                    <Button Content="⏭ Next" Click="Next_Click" Width="80"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Active Sessions" FontSize="18" VerticalAlignment="Center" FontWeight="SemiBold"/>
            <Button Content="Refresh" Margin="15,0,0,0" Click="Refresh_Click"/>
        </StackPanel>

        <ListView x:Name="SessionsList" Grid.Row="3" SelectionChanged="SessionsList_SelectionChanged">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Padding="10">
                        <TextBlock Text="{Binding SourceAppId}" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding Title}" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" FontSize="12"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>